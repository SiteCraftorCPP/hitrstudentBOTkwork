# Telegram Bot - HitrStudent Bot

Telegram бот для заработка на подписках, рефералах и просмотре стримов.

## 🚀 Быстрый старт

### Локальная установка

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd hitrstudentBOTkwork
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp env.example .env
# Отредактируйте .env файл и укажите ваш BOT_TOKEN и другие настройки
```

5. **Запустите бота:**
```bash
python main.py
```

## 📋 Требования

- Python 3.8+
- Telegram Bot Token (получить у @BotFather)

## 🔧 Настройка

### Переменные окружения (.env)

Создайте файл `.env` на основе `env.example`:

- `BOT_TOKEN` - токен бота от @BotFather
- `ADMINS` - ID администраторов (через запятую)
- `WITHDRAWAL_CHANNEL_ID` - ID канала для уведомлений о выводах
- Остальные параметры опциональны (используются значения по умолчанию)

## 🖥️ Деплой на VPS

### Вариант 1: Systemd сервис (рекомендуется)

1. **Скопируйте проект на сервер:**
```bash
scp -r . user@your-server:/opt/telegram-bot/
```

2. **На сервере установите зависимости:**
```bash
cd /opt/telegram-bot
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

3. **Создайте .env файл:**
```bash
cp env.example .env
nano .env  # Отредактируйте настройки
```

4. **Создайте systemd сервис:**
```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

Вставьте содержимое из `telegram-bot.service` файла, изменив пути при необходимости.

5. **Запустите сервис:**
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

6. **Проверьте статус:**
```bash
sudo systemctl status telegram-bot
```

7. **Просмотр логов:**
```bash
sudo journalctl -u telegram-bot -f
# или
tail -f /opt/telegram-bot/logs/bot.log
```

### Вариант 2: Docker (опционально)

```bash
docker-compose up -d
```

## 📁 Структура проекта

```
.
├── main.py              # Точка входа
├── config.py            # Конфигурация (загружает .env)
├── database.py          # Работа с БД
├── keyboards.py         # Клавиатуры бота
├── handlers/            # Обработчики
│   ├── start.py        # Команды /start
│   ├── callbacks.py    # Callback обработчики
│   ├── admin.py        # Админ-панель
│   └── admin_earn.py   # Настройки раздела "Заработать"
├── logs/               # Логи бота
├── bot_database.db     # База данных SQLite
├── requirements.txt    # Зависимости
└── .env               # Переменные окружения (не в git)
```

## 🔐 Безопасность

- **НЕ коммитьте** файл `.env` в Git
- Токен бота храните только в `.env`
- Регулярно делайте бэкапы базы данных

## 📝 Логирование

Логи сохраняются в:
- `logs/bot.log` - файл логов
- Консоль - для отладки

## 🛠️ Админ-панель

Доступна через команду `/admin` (только для пользователей из `ADMINS`):
- Управление каналами
- Настройка наград
- Статистика
- Настройка текстов и кнопок

## 📊 База данных

Используется SQLite. Файл БД: `bot_database.db`

**Важно:** Регулярно делайте бэкапы БД перед обновлениями!

## 🔄 Обновление

1. Остановите бота:
```bash
sudo systemctl stop telegram-bot
```

2. Сделайте бэкап БД:
```bash
cp bot_database.db bot_database.db.backup
```

3. Обновите код:
```bash
git pull
```

4. Обновите зависимости (если нужно):
```bash
source venv/bin/activate
pip install -r requirements.txt
```

5. Запустите бота:
```bash
sudo systemctl start telegram-bot
```

## 🐛 Отладка

Если бот не запускается:

1. Проверьте логи:
```bash
tail -f logs/bot.log
```

2. Проверьте переменные окружения:
```bash
cat .env
```

3. Проверьте статус сервиса:
```bash
sudo systemctl status telegram-bot
```

## 📞 Поддержка

При возникновении проблем проверьте:
- Правильность токена бота
- Доступность интернета на сервере
- Права доступа к файлам и директориям
- Логи в `logs/bot.log`

## 📄 Лицензия

Проект для личного использования.

